import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shieldintelligence.shieldauth',
  appName: 'SHIELD-Authenticator',
  webDir: 'build'
};

export default config;
